version: '3.9'
services:   
   app:
      build:
         context: ./
         dockerfile: Dockerfile
      volumes:
         - '.:/home/node/app'
      command: npm run start:dev
      ports:
         - "4200:3000"